/**
 * Extension bundle entry point.
 * Exports all extension components under default.components.
 */

import { CustomHomeDashboard } from "./CustomHomeDashboard";

export default {
  components: {
    CustomHomeDashboard,
  },
};
